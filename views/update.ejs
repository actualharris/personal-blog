<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Post</title>

    <style>
        /* Basic styling for the create page */
        .container {
            margin: 20px auto;
            width: 80%;
        }
        .nav-area ul {
            list-style-type: none;
        }
        .nav-area ul li {
            display: inline;
            margin-right: 10px;
        }

        .navigation a{
            text-decoration: none;
        }
        
        .form {
            margin-top: 20px;
        }
        .form label, .form input, .form textarea {
            display: block;
            margin-bottom: 10px;
            width: 100%;
        }
        .form-submit {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            cursor: pointer;
        }
    </style>
    
</head>

<body>
    <div class="container">
        <div class="nav-area">
            <ul class="navigation">
                <li id="home"><a href="/">Home</a></li>
                <li id="create"><a href="/create">Create</a></li>
            </ul>
        </div>
        <div class="post-area">
            <form action="/api/posts/<%=post.id%>"  method="post">
                <label for="post-title">Title:</label>
                <input type="text" id="title" name="title" value="<%= post.title %>" required>

                <label for="post-content">Content:</label>
                <textarea rows="15" id="content" name="content" required><%= post.content %></textarea>

                <button type="submit">Update</button>
            </form>
        </div>
    </div>
</body>
</html>